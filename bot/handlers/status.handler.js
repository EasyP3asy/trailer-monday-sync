// bot/handlers/status.handler.js
// Handles the /status command — returns latest trailer data.
// Placeholder — implement when bot is ready.

export function registerStatusHandler(bot) {
  // bot.onText(/\/status (.+)/, async (msg, match) => {
  //   const trailerNumber = match[1];
  //   // query DB for trailer, send back location + last update time
  // });
}