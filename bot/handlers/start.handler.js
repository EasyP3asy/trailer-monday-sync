// bot/handlers/start.handler.js
// Handles the /start command.
// Placeholder — implement when bot is ready.

export function registerStartHandler(bot) {
  // bot.onText(/\/start/, (msg) => {
  //   bot.sendMessage(msg.chat.id, 'Welcome to the Fleet Tracker bot!');
  // });
}